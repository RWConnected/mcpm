{
  "name": "@mcpm/cli",
  "version": "1.0.7",
  "type": "module",
  "bin": {
    "mcpm": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run --watch src/index.ts --",
    "build": "bun build --compile --minify src/index.ts --outfile ../../dist/mcpm",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@mcpm/core": "workspace:*",
    "commander": "^13.0.0",
    "picocolors": "^1.1.0",
    "@inquirer/prompts": "^7.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
